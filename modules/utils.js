exports.getDate = function() {
    const date = new Date(); // Create a new Date object representing the current date and time.
    return date.toString(); // Convert the Date object to a string and return it.
}
