//Chatgpt, github Copilot used for coding, debugging, refactoring, and documentation.

// Import the custom math module from the specified path.
const mo = require('./modules/math');

// Uses the add and subtract function function from the math module to add two numbers and print the result.
console.log(`Hello Brendan Doyle. The result of the addition is = ${mo.add(5, 3)}`);
console.log(`Hello Brendan Doyle. The result of the subtraction is = ${mo.subtract(6, 4)}`); 